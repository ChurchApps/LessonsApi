export interface Schedule {
  id?: string;
  churchId?: string;
  classroomId?: string;
  displayName?: string;
  externalProviderId?: string;
  lessonId?: string;
  programId?: string;
  scheduledDate?: Date;
  studyId?: string;
  venueId?: string;
}
