export interface Lesson {
  id?: string;
  churchId?: string;
  studyId?: string;
  name?: string;
  slug?: string;
  title?: string;
  sort?: number;
  image?: string;
  live?: boolean;
  description?: string;
  videoEmbedUrl?: string;
}
